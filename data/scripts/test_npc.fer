func on_interact() {
    play_sound("turn_pages");
    
    if (get_flag("has_met_npc") == true) {
        show_dialogue("Ya nos conocemos, viajero.");
    } else {
        show_dialogue("¡Hola! Soy un archivo .fer cargado desde el disco.");
        set_flag("has_met_npc", true);
    }
}