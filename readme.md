# How I made things now in FER

Since I can call systems into fer, if I wanto to make a teleport, I just call the method request_teleport from GameState (the same thing with changing level).

If I want to change sound I call the method play_music from ResourceManager.